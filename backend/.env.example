# Copy to .env and fill with environment-specific values
# Mongo connection string. If Railway still uses DATABASE_URL from the
# previous Prisma setup, point that variable to the same value or export
# MONGO_URI directly.
MONGO_URI="mongodb+srv://user:password@cluster.mongodb.net/hashequity"
JWT_SECRET="replace-with-secure-secret"
PORT=8080
