# Copy to .env and adjust the values for your environment
VITE_CHAIN_ID=137
VITE_RPC_URL=https://polygon-rpc.com
VITE_HASH_TOKEN_ADDRESS=0xYourHashTokenAddress
VITE_NFT_CONTRACT_ADDRESS=0xYourNftContractAddress
# When deploying the frontend separately from the backend, point this to the backend service URL
# Leave blank to use same-origin calls (useful for local development with a proxy).
VITE_BACKEND_URL=https://your-backend-service.up.railway.app
