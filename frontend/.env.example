# Connect to backend API
VITE_API_BASE_URL=https://api.hashequity.com

# Connect to MongoDB if used client-side (optional)
VITE_MONGO_URL=mongodb://mongo:YourMongoPassword@mongodb.railway.internal:27017

# Auth secret for JWT if decoding client-side (optional — usually backend only)
VITE_JWT_SECRET=supersecretkey
